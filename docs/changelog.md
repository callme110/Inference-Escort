# Changelog

## Initial Inference-Escort Snapshot

- Renamed the project package to `inference-escort`.
- Renamed the Python import package to `inference_escort`.
- Renamed the API package to `inference-escort-api`.
- Removed upstream Git history, contribution metadata, and repository-specific GitHub configuration.
